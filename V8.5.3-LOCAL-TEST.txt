FOURTH & PICK V8.5.3 — POOL MODE PERSISTENCE FIX

Fix verified at the mode-state layer:
- All 10 modes unchecked are explicitly saved false.
- Reload preserves all 10 false values.
- Re-enabling one mode restores only that mode.
- Player Pool Game Hub renders only enabled modes.
- Direct UI access is blocked for disabled modes.
- Pick'em/Survivor server write routes and extra-mode write route enforce disabled state.
- Historical pool data is preserved.

LOCAL TEST:
1. Extract to a NEW folder.
2. Run START-WINDOWS.bat (it will install dependencies if needed).
3. Open Commissioner HQ and sign in.
4. Uncheck ALL 10 pool modes and click Save Pool Settings.
5. Return to Pool Game Hub. It should say no pool games are enabled.
6. Return to Commissioner HQ, enable only one mode, save, then return to Pool Game Hub. Only that mode should appear.
