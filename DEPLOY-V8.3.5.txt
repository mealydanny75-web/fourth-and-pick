FOURTH & PICK V8.3.5 — LIVE DEPLOYMENT

1. Back up/export your live pool data before a production update.
2. Upload/replace the repository files with this build.
3. Keep the existing DATABASE_URL unchanged.
4. Keep RESEND_API_KEY and EMAIL_FROM if real email is configured.
5. Render build command: npm install
6. Render start command: npm start
7. Deploy, then test: account login, My Pools, Find Pool, join request, approve/deny, Leave Pool, transfer commissioner, picks, Live HQ, Game Detail Center, Communications.

No destructive database reset is required. V8.3.5 stores privacy/join-request settings inside each pool JSON record and uses the existing memberships table.
