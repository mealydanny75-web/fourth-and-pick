FOURTH & PICK V8.4.5 — COMMISSIONER + EMAIL FIX

Fixes:
- Restores missing loadCommissionerV83() used by Commissioner HQ.
- Resend can use its test sender when EMAIL_FROM is omitted or set to Fourth & Pick <onboarding@resend.dev>.
- Adds optional EMAIL_REPLY_TO for your personal email address.
- Resend rejection messages now include the HTTP status and provider message for easier troubleshooting.

Render environment for testing:
DATABASE_URL = keep your existing value
RESEND_API_KEY = your Resend API key
EMAIL_FROM = Fourth & Pick <onboarding@resend.dev>
EMAIL_REPLY_TO = your personal email address

IMPORTANT: Resend's onboarding/test sender can be restricted to the email address associated with your Resend account. For sending to all pool members, verify a domain you own and change EMAIL_FROM to an address on that domain.

No database reset or migration is required.
