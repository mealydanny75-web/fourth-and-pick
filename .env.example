# Leave DATABASE_URL unset for local testing (data.json).
# For a deployed V6 app, set DATABASE_URL to your PostgreSQL connection string.
DATABASE_URL=
PORT=3000
# Set PGSSL=disable only if your database explicitly requires non-SSL local connections.
# PGSSL=disable
